<h2>Upravenie ponuky</h2>

<%= render "form.html", 
    changeset: @changeset,
    action: job_path(@conn, :update, @changeset.data.token),
    categories: @categories,
    
    button_text: "Upraviť ponuku a pokračovať na náhľad",
    errors: @changeset.errors %>